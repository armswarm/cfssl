{
    "signing": {
        "default": {
            "auth_remote": {
                "remote": "server",
                "auth_key": "client"
            }
        }
    },
    "auth_keys": {
        "client": {
            "type": "standard",
            "key": "${CFSSL_AUTH_KEY}"
        }
    },
    "remotes": {
        "server": "${CFSSL_HOST}"
    }
}
